# Secrets and live runtime state
stream.key
token.json
client_secret*.json
broadcast.id
stream.id
thumbnail_set.id
*.log
*.swp

# Python/build/runtime clutter
__pycache__/
*.py[cod]
.venv/
venv/
scratch/

# Local/private backups copied from a Pi may contain secrets
backups/
*.raw.backup
*.bak
*.tar.gz
